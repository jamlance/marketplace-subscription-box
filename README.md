# marketplace-subscription-box

Deploy repo for the **subscription-box** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs subscription-box --push`.

Coolify: base_directory `/`, dockerfile `/apps/subscription-box/Dockerfile`.
